[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."proxy"]
  node_bundler = "esbuild"

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
